# Marinboy Backend

- 실행: `mvn spring-boot:run` (`http://127.0.0.1:8082`)
- 테스트: `mvn test`
- 구조: `Controller → Service → Mapper/MyBatis XML → Oracle`
- Next.js 연결: `/` 요청은 `http://127.0.0.1:3000`으로 이동합니다.
