spring.application.name=marinboy-v2
server.port=${V2_SERVER_PORT:8081}

# v2는 서버 세션 방식입니다. v3와 같은 호스트에서 실행해도 쿠키 이름이 겹치지 않게 분리합니다.
server.servlet.session.cookie.name=MARINBOY_V2_SESSION
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=lax

# Oracle XE
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.url=${V2_ORACLE_URL:${ORACLE_URL:jdbc:oracle:thin:@localhost:1521/XEPDB1}}
spring.datasource.username=${V2_ORACLE_USERNAME:${ORACLE_USERNAME:marinboy}}
spring.datasource.password=${V2_ORACLE_PASSWORD:${ORACLE_PASSWORD:marinboy}}

# MyBatis
mybatis.type-aliases-package=com.marinboy.dto
mybatis.mapper-locations=classpath:mybatis/mapper/**/*.xml
mybatis.configuration.map-underscore-to-camel-case=true

# Thymeleaf
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false

# Logging
logging.level.com.marinboy=info
logging.level.org.mybatis=warn
logging.level.org.apache.ibatis=warn
logging.level.org.springframework.jdbc=warn

# Kakao OAuth2 (Kakao Developers REST API key)
spring.security.oauth2.client.registration.kakao.client-id=${KAKAO_CLIENT_ID:not-configured}
spring.security.oauth2.client.registration.kakao.client-secret=${KAKAO_CLIENT_SECRET:}
spring.security.oauth2.client.registration.kakao.client-authentication-method=client_secret_post
spring.security.oauth2.client.registration.kakao.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.kakao.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
spring.security.oauth2.client.registration.kakao.scope=profile_nickname
spring.security.oauth2.client.registration.kakao.client-name=Kakao
spring.security.oauth2.client.provider.kakao.authorization-uri=https://kauth.kakao.com/oauth/authorize
spring.security.oauth2.client.provider.kakao.token-uri=https://kauth.kakao.com/oauth/token
spring.security.oauth2.client.provider.kakao.user-info-uri=https://kapi.kakao.com/v2/user/me
spring.security.oauth2.client.provider.kakao.user-name-attribute=id

# Admin service image upload limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=60MB

# 실행 폴더가 바뀌어도 같은 업로드 폴더를 사용합니다.
app.upload.directory=${V2_UPLOAD_DIRECTORY:${user.home}/.marinboy/uploads}
