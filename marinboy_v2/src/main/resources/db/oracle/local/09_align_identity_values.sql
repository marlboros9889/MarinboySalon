-- 명시적 ID로 적재한 샘플 데이터보다 IDENTITY 다음 값이 낮지 않도록 정렬합니다.
ALTER TABLE MB_USER MODIFY ID GENERATED BY DEFAULT AS IDENTITY (START WITH LIMIT VALUE);
ALTER TABLE MB_SERVICE_ITEM MODIFY ID GENERATED BY DEFAULT AS IDENTITY (START WITH LIMIT VALUE);
ALTER TABLE MB_SERVICE_IMAGE MODIFY ID GENERATED BY DEFAULT AS IDENTITY (START WITH LIMIT VALUE);
ALTER TABLE MB_RESERVATION MODIFY ID GENERATED BY DEFAULT AS IDENTITY (START WITH LIMIT VALUE);

COMMIT;
