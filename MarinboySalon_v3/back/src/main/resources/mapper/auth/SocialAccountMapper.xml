<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.marinboy.auth.repository.SocialAccountMapper">
    <!-- selectByProviderUser: 이미 연결된 소셜 회원을 조회합니다. -->
    <select id="selectByProviderUser" resultType="SocialAccount">
        SELECT id, user_id, provider, provider_user_id, provider_email
        FROM user_social_account
        WHERE provider = #{provider}
          AND provider_user_id = #{providerUserId}
    </select>
    <!-- insert: 새 소셜 계정 연결 정보를 저장합니다. -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_social_account
            (user_id, provider, provider_user_id, provider_email)
        VALUES
            (#{userId}, #{provider}, #{providerUserId}, #{providerEmail})
    </insert>
</mapper>
