<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.marinboy.user.repository.AppUserMapper">

    <!-- selectById: 회원 번호로 한 명을 조회합니다. -->
    <select id="selectById" resultType="AppUser">
        SELECT id, email, password, name, phone, role, created_at
        FROM user_account
        WHERE id = #{id}
          AND deleted_at IS NULL
    </select>

    <!-- selectByEmail: 로그인할 회원을 이메일로 조회합니다. -->
    <select id="selectByEmail" resultType="AppUser">
        SELECT id, email, password, name, phone, role, created_at
        FROM user_account
        WHERE email = #{email}
          AND deleted_at IS NULL
    </select>

    <!-- insert: 비밀번호를 BCrypt로 암호화한 회원을 등록합니다. -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_account (email, password, name, phone, role)
        VALUES (#{email}, #{password}, #{name}, #{phone}, #{role})
    </insert>

    <!-- update: 본인의 이름과 연락처를 수정합니다. -->
    <update id="update">
        UPDATE user_account
        SET name = #{name}, phone = #{phone}
        WHERE id = #{id}
    </update>

    <!-- countByEmail: 이메일 중복을 확인합니다. -->
    <select id="countByEmail" resultType="int">
        SELECT COUNT(*)
        FROM user_account
        WHERE email = #{email}
    </select>

    <select id="selectAll" resultType="AppUser">
        SELECT id, email, password, name, phone, role, created_at
        FROM user_account
        WHERE deleted_at IS NULL
        ORDER BY created_at DESC
    </select>

    <update id="updateRole">
        UPDATE user_account SET role = #{role} WHERE id = #{id}
    </update>

    <update id="updateAdminUser">
        UPDATE user_account SET email = #{email}, name = #{name}, phone = #{phone} WHERE id = #{id}
    </update>

    <!-- 접수 중인 예약만 탈퇴를 막고, 완료·취소 이력은 보존합니다. -->
    <select id="countRequestedReservations" resultType="int">
        SELECT COUNT(*) FROM reservation
        WHERE user_id = #{userId}
          AND status = 'REQUESTED'
    </select>

    <!-- 예약·리뷰 이력의 외래키를 보존하면서 로그인과 개인정보를 제거합니다. -->
    <update id="withdrawById">
        UPDATE user_account
        SET email = CONCAT('withdrawn-', id, '-', UNIX_TIMESTAMP(), '@deleted.local'),
            password = 'WITHDRAWN',
            name = '탈퇴 회원',
            phone = '',
            deleted_at = NOW()
        WHERE id = #{id}
          AND deleted_at IS NULL
    </update>

    <delete id="deleteSocialAccountsByUserId">
        DELETE FROM user_social_account WHERE user_id = #{userId}
    </delete>
</mapper>
