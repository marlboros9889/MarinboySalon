<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.marinboy.discountevent.repository.DiscountEventMapper">
    <select id="selectAll" resultType="DiscountEvent">
        SELECT id, name, start_date, end_date, discount_rate, created_at
        FROM discount_event
        ORDER BY start_date DESC, id DESC
    </select>

    <select id="selectById" resultType="DiscountEvent">
        SELECT id, name, start_date, end_date, discount_rate, created_at
        FROM discount_event WHERE id = #{id}
    </select>

    <!-- 예약 생성 날짜가 기간 안인 이벤트 한 건만 금액 계산에 사용합니다. -->
    <select id="selectActiveByDate" resultType="DiscountEvent">
        SELECT id, name, start_date, end_date, discount_rate, created_at
        FROM discount_event
        WHERE #{date} BETWEEN start_date AND end_date
        ORDER BY id DESC
        LIMIT 1
    </select>

    <select id="countOverlappingPeriod" resultType="int">
        SELECT COUNT(*) FROM discount_event
        WHERE start_date <![CDATA[ <= ]]> #{endDate}
          AND end_date <![CDATA[ >= ]]> #{startDate}
        <if test="excludeId != null">AND id != #{excludeId}</if>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO discount_event (name, start_date, end_date, discount_rate)
        VALUES (#{name}, #{startDate}, #{endDate}, #{discountRate})
    </insert>

    <update id="update">
        UPDATE discount_event
        SET name = #{name}, start_date = #{startDate}, end_date = #{endDate}, discount_rate = #{discountRate}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">DELETE FROM discount_event WHERE id = #{id}</delete>
</mapper>
